export default function Title({mainTitle}) {
    return (
        <div>
            
            <h1 style={{color : "black", borderBottom : '4px solid gray'}}>
                {mainTitle}
                </h1>
        </div>
    )
}